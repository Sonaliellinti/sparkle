# ── Web framework ───────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1

# ── Database ────────────────────────────────────────────────────────────
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10   # Postgres driver — only used when DATABASE_URL points at Postgres/Supabase

# ── Auth ────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1   # passlib 1.7.4's bcrypt handler is incompatible with bcrypt>=4.1 (self-test crash)
python-multipart==0.0.20   # required by OAuth2PasswordRequestForm

# ── Settings / validation ───────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# ── AI layer (wired up in Phase 3 — installed now so env is ready) ─────
sentence-transformers==3.3.1
networkx==3.4.2
groq==0.13.1
